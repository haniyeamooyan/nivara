# Nivara — MVP Product Specification

**Product:** Internal AI/SaaS Budget and Purchase Management  
**Status:** MVP implementation complete through Phase 9; UI redesign and subsequent product logic changes pending.
**Version:** 0.1

## 1. Product direction

یک سیستم متمرکز برای ثبت درخواست‌ها و خریدهای AI/SaaS، تخصیص بودجه افراد و تیم‌ها، نگهداری تاریخچه و ارائه گزارش به CTO.

## 2. Evidence and certainty

### FACT

- درخواست‌ها اکنون حضوری یا از طریق Telegram به مدیر اعلام می‌شوند.
- خریدها در Sheetهای جداگانه تیم‌ها ثبت می‌شوند.
- بودجه در دوره‌های ماهانه تخصیص داده می‌شود و مقدار آن می‌تواند برای افراد و تیم‌ها متفاوت باشد.
- مدیر خرید را انجام می‌دهد و CTO گزارش کل شرکت را می‌خواهد.

### DECISION

- MVP از تخصیص دستی بودجه در هر دوره استفاده می‌کند؛ شارژ خودکار وجود ندارد.
- بودجه دوره پس از شروع دوره قابل تغییر نیست و تغییرات برای دوره بعد ثبت می‌شوند.
- تا زمان تکمیل خرید، بودجه رزرو یا کسر نمی‌شود.
- خرید مازاد بر اعتبار مجاز است و سهم مازاد کارمند خارج از سیستم پرداخت می‌شود.
- اعلان‌های MVP فقط داخل سیستم هستند.
- حساب خریدهای غیرشخصی هر تیم از موجودی کارکنان جداست؛ مدیر پس از دریافت بودجه خارج از سامانه، شارژ را دستی ثبت می‌کند. ماندهٔ تیم در دفترکل می‌ماند و به ماه بعد منتقل می‌شود؛ خرید فقط تا سقف ماندهٔ جاری قابل ثبت است.

### HYPOTHESIS

متمرکزسازی داده‌های خرید و بودجه، گزارش‌گیری CTO و پیگیری مدیران را نسبت به Sheetهای پراکنده ساده‌تر می‌کند. این فرضیه پس از استفاده واقعی باید سنجیده شود.

### UNKNOWN

- قابلیت اتصال مستقیم به سرویس‌های خارجی، اطلاعات مصرف واقعی، پرداخت آنلاین و SSO در MVP لازم نیستند.

## 3. Users and permissions

### Super Admin

- ایجاد و ویرایش تیم‌ها
- تعیین مدیر هر تیم
- مدیریت ساختار سازمانی
- ثبت مستقیم خرید شخصی از اعتبار حساب خودش؛ بدون درخواست یا بررسی مدیر

### Manager

- مشاهده اعضا و خریدهای تیم خود
- افزودن، ویرایش و تکمیل اعضای تیم
- تخصیص بودجه اعضا
- بررسی و خرید یا رد درخواست‌های شخصی
- ثبت مستقیم خرید شخصی از اعتبار حساب خودش؛ بدون ثبت درخواست برای خودش
- ثبت خریدهای غیرشخصی تیم
- ثبت تمدید یا عدم تمدید

### Employee

- مشاهده موجودی و تخصیص بودجه خود
- ثبت درخواست خرید
- لغو درخواست پیش از خرید
- ویرایش و ارسال مجدد درخواست ردشده یا لغوشده
- ثبت درخواست عدم تمدید

### CTO

- مشاهده اطلاعات کل شرکت
- مشاهده گزارش‌های ماهانه و فصلی
- مشاهده تاریخچه و audit log
- ثبت درخواست یا خرید شخصی در سامانه ندارد

**قانون تفکیک جریان شخصی:** فقط Employee می‌تواند `PurchaseRequest` ثبت کند و درخواست او در صف مدیر همان تیم قرار می‌گیرد. Manager و Super Admin خرید شخصی را مستقیماً ثبت می‌کنند؛ سهم شرکت از حساب خود خریدار کسر می‌شود و هر مبلغ مازاد به‌عنوان سهم شخصی ثبت می‌شود. خرید Super Admin به تیمی نسبت داده نمی‌شود. CTO فقط مشاهده‌گر گزارش‌هاست و مسیر خرید شخصی ندارد.

## 4. Budget rules

- هر کاربر فقط عضو یک تیم است و هر تیم یک مدیر دارد.
- مدیر می‌تواند یک مبلغ پایه برای همه اعضای تیم تعیین کند.
- مدیر می‌تواند مبلغ افراد خاص را به‌صورت دستی override کند.
- تخصیص استفاده‌نشده به دوره بعد منتقل می‌شود.
- تخصیص دوره جدید فقط پس از تأیید مدیر به موجودی اضافه می‌شود.
- تخصیص هر تیم پس از تأیید مدیر همان تیم قفل می‌شود؛ قفل یک تیم نباید مانع ثبت تخصیص اولیهٔ تیم‌های دیگر در همان ماه شود.
- در MVP فقط دورهٔ ماه جاری قابل تخصیص است؛ دوره‌های گذشته صرفاً برای مشاهده‌اند و اعتبار آینده زودتر قابل‌خرج نمی‌شود.
- مانده در دفترکل حساب باقی می‌ماند؛ carry-over در تخصیص ماه جدید به‌صورت snapshot ثبت می‌شود و تراکنش اعتباری تکراری ایجاد نمی‌کند.
- خرید تکمیل‌شده با تخصیص معتبر همان دوره محاسبه می‌شود.
- مبلغ واقعی خرید می‌تواند توسط مدیر اصلاح شود.
- مبلغ مصرفی شرکت برابر است با کمینه مبلغ واقعی خرید و موجودی قابل‌استفاده.
- مبلغ مازاد به‌عنوان سهم پرداختی کارمند ثبت می‌شود و پرداخت آن خارج از سیستم است.

## 5. Personal purchase workflow

```text
Employee submits request
        ↓
Manager reviews
   ┌────┴─────┐
Purchased   Rejected
   │
Account period recorded

Employee may cancel before purchase.
```

Request fields:

- سرویس یا agent
- مبلغ تقریبی
- توضیح یا نیاز خرید
- هر درخواست مربوط به یک سرویس/اکانت است؛ برای چند اکانت درخواست جداگانه ثبت می‌شود.

Purchase fields:

- مبلغ واقعی
- خریدار
- تاریخ خرید
- تاریخ شروع و پایان پلن
- اعتبار مصرف‌شده شرکت
- سهم مازاد کارمند
- وضعیت تمدید

رد درخواست نیازمند ثبت دلیل است.

## 6. Non-personal purchases

- فقط مدیر می‌تواند برای حساب تیم شارژ دستی ثبت کند یا خرید غیرشخصی انجام دهد.
- حساب تیم از اعتبار فردی اعضا جداست و هر خرید به تیم اختصاص داده می‌شود، نه به کارمند.
- شارژهای تیم در دفترکل ثبت می‌شوند؛ ماندهٔ خرج‌نشده خودکار در حساب باقی می‌ماند و به دوره‌های بعد منتقل می‌شود.
- مبلغ هنگام ثبت خرید به‌صورت اتمیک با موجودی تیم کنترل و از همان حساب کسر می‌شود؛ خرج بیش از مانده پذیرفته نمی‌شود.
- مبلغ شارژ دستی موجودی قابل خرج فعلی تیم را تعیین می‌کند؛ سقف جداگانه و شارژ خودکار ماهانه در MVP نداریم.
- تاریخ پایان و وضعیت تمدید اختیاری است.
- تاریخ پایان و وضعیت تمدید اختیاری است.

## 7. Renewal and notifications

- تمدید، خرید جدید محسوب می‌شود.
- کارمند می‌تواند درخواست عدم تمدید ثبت کند.
- درخواست عدم تمدید برای history و اطلاع مدیر است؛ لغو سرویس خارج از سیستم انجام می‌شود.
- سه روز پیش از پایان پلن، اعلان داخلی برای کارمند و مدیر مربوطه نمایش داده می‌شود.
- کارمند می‌تواند برای پلن شخصی فعال درخواست عدم تمدید بدهد یا درخواست در انتظار را لغو کند؛ مدیر درخواست را بررسی می‌کند و تصمیم در audit log ثبت می‌شود.
- ثبت تمدید توسط مدیر یک رکورد خرید جدید می‌سازد و پلن قبلی را `RENEWED` می‌کند؛ پلن جدید به رکورد قبلی پیوند دارد و سهم بودجه طبق نوع حساب محاسبه می‌شود.
- مدیر پس از تاریخ پایان، وضعیت پلن فعال را `EXPIRED` می‌کند؛ سابقهٔ قبلی حفظ می‌شود.
- job یادآوری روزانه فقط سه روز مانده به پایان پلن فعال اعلان می‌سازد؛ شناسهٔ یکتای dedupe از ایجاد اعلان تکراری جلوگیری می‌کند.
- در سرور پایدار Next.js، job هنگام شروع برنامه یک‌بار اجرا و سپس هر روز ساعت 00:05 UTC زمان‌بندی می‌شود. اجرای دستی از `npm run reminders:expiry` یا `POST /api/cron/expiry-reminders` با `Authorization: Bearer $CRON_SECRET` نیز ممکن است؛ در محیط serverless باید endpoint را با scheduler ارائه‌دهنده فراخوانی کرد.

## 8. Budget allocation flow

```text
New period becomes available
        ↓
In-app notification to manager
        ↓
Manager sets team default
        ↓
Manager edits individual exceptions
        ↓
Manager confirms allocations
        ↓
Balances become available to employees
```

## 9. CTO reporting

Reports must support filtering by تیم، فرد، سرویس و بازه زمانی.

Minimum metrics:

- هزینه هر تیم
- سرویس‌ها و agentهای خریداری‌شده
- هزینه‌های شخصی و غیرشخصی
- تخصیص، مصرف و موجودی باقی‌مانده
- سهم مازاد پرداختی کارکنان
- خریدهای فعال و نزدیک به پایان
- تاریخچه تغییرات و audit log

در MVP، نمایش داخل سامانه کافی است و export لازم نیست.

**گزارش CTO:** خریدها بر اساس تاریخ شروع ثبت‌شدهٔ پلن در بازه شمرده می‌شوند؛ تخصیص‌های فردی بر اساس زمان تأیید، شارژ تیمی بر اساس زمان تراکنش و موجودی به‌صورت snapshot زمان مشاهده محاسبه می‌شود. فیلتر سرویس بر بودجه یا مانده اثر ندارد چون حساب بودجه به سرویس خاص متصل نیست. فهرست audit بر اساس بازه و فیلتر فرد (به‌عنوان انجام‌دهنده) محدود است؛ فیلتر تیم/سرویس روی آن اعمال نمی‌شود.

## 10. Out of scope for MVP

- Telegram، Slack یا email integration
- پرداخت آنلاین
- اتصال به API سرویس‌های AI/SaaS
- usage analytics
- automatic provisioning
- مدیریت اطلاعات login یا email واقعی اکانت‌ها
- invoice management
- SSO
- multi-tenancy تجاری
- marketplace سرویس‌ها
- تشخیص خودکار اکانت‌های بدون استفاده

## 11. Acceptance criteria

- مدیر می‌تواند برای یک دوره، بودجه پایه تیم و override فردی تعیین و تأیید کند.
- کارمند فقط وقتی ماندهٔ قابل‌استفاده‌اش بیشتر از صفر است می‌تواند درخواست جدید ثبت یا درخواست رد/لغوشده را دوباره ارسال کند؛ موجودی صفر یا منفی درخواست را مسدود می‌کند.
- ثبت درخواست، بودجه را کم نمی‌کند.
- تکمیل خرید، مبلغ واقعی، سهم شرکت و سهم مازاد را ثبت می‌کند.
- درخواست ردشده یا لغوشده با دلیل/وضعیت قابل مشاهده و ارسال مجدد است.
- بودجه دوره شروع‌شده قابل ویرایش نیست.
- CTO می‌تواند هزینه‌ها را با فیلترهای تعریف‌شده مشاهده کند.
- تغییرات مهم کاربران، بودجه، درخواست‌ها و خریدها در audit log ثبت می‌شوند.
